// import CryptoJS from "crypto-js";
// import config from "src/scope/rsl-redux/config";

const Crypto = {
    // encrypt(data) {
    //     const ciphertext = CryptoJS.AES.encrypt(data, config.CRYPTO_SECRET).toString();
    //     return ciphertext
    // },
    //
    // decrypt(ciphertext) {
    //     const bytes = CryptoJS.AES.decrypt(ciphertext, config.CRYPTO_SECRET);
    //     const originalText = bytes.toString(CryptoJS.enc.Utf8);
    //     return originalText
    // }
}

export default Crypto